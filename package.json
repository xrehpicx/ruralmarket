{
  "name": "ruralmarket",
  "version": "1.0.0",
  "description": "",
  "main": "server.js",
  "scripts": {
    "dev": "nodemon --ignore client/ server.js",
    "start": "node server.js",
    "client-build": "cd client && npm run build",
    "deploy": "npm run client-build && git add . && git commit -m auto-deploy && git push",
    "sdeploy": "git add . && git commit -m auto-deploy && git push"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "mongoose": "^5.10.5",
    "socket.io": "^2.3.0",
    "web-push": "^3.4.4"
  },
  "devDependencies": {
    "nodemon": "^2.0.6"
  }
}
